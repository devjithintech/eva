{
  "name": "lighthouse-candidate-intelligence",
  "private": true,
  "version": "0.1.0",
  "description": "LightHouse Candidate Intelligence Agent — an AG-UI generative-UI app (Vite/React frontend + Express agent backend).",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.17"
  }
}
