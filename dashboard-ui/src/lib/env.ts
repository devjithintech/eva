/** URL of the conversational (LightAssist chat) app — the sibling package this
 *  dashboard links back to as "Home". */
export const CONVERSATION_URL = import.meta.env.VITE_CONVERSATION_URL ?? "http://localhost:5173";
